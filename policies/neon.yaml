#  ─────────────────────────────────────────────────────────
#  Policy: Neon MCP Server
#  Server: neondatabase/mcp-server-neon
#  Homepage: https://github.com/neondatabase/mcp-server-neon
#  Tags: neon, postgres, serverless, database, branching
#  Tools: 29
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for neondatabase/mcp-server-neon"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  complete_query_tuning: Apply tuning changes
    complete_query_tuning:
        rules: []
    #  describe_branch: Fetch branch details
    describe_branch:
        rules: []
    #  describe_project: Retrieve detailed project information
    describe_project:
        rules: []
    #  describe_table_schema: Get detailed table structure
    describe_table_schema:
        rules: []
    #  fetch: Retrieve specific resource details
    fetch:
        rules: []
    #  get_connection_string: Return database connection details
    get_connection_string:
        rules: []
    #  get_database_tables: List database tables
    get_database_tables:
        rules: []
    #  get_doc_resource: Fetch documentation pages
    get_doc_resource:
        rules: []
    #  list_branch_computes: Display compute endpoints for a branch
    list_branch_computes:
        rules: []
    #  list_docs_resources: Index documentation
    list_docs_resources:
        rules: []
    #  list_organizations: Show accessible organisations
    list_organizations:
        rules: []
    #  list_projects: List Neon projects in account
    list_projects:
        rules: []
    #  list_shared_projects: Display projects shared with current user
    list_shared_projects:
        rules: []
    #  list_slow_queries: Identify query bottlenecks
    list_slow_queries:
        rules: []
    #  prepare_query_tuning: Analyse query optimisations
    prepare_query_tuning:
        rules: []
    #  search: Find organisations, projects, and branches
    search:
        rules: []
    #  ── Write Tools ───────────────────────────────────────────
    #  create_branch: Create a development branch within a project
    create_branch:
        rules: []
    #  create_project: Create a new Neon project
    create_project:
        rules: []
    #  ── Execute Tools ───────────────────────────────────────────
    #  run_sql: Execute individual SQL queries
    run_sql:
        rules: []
    #  run_sql_transaction: Run multiple queries in a transaction
    run_sql_transaction:
        rules: []
    #  ── Destructive Tools ───────────────────────────────────────────
    #  delete_branch: Remove an existing branch
    delete_branch:
        rules: []
    #  delete_project: Remove a project and its resources
    delete_project:
        rules: []
    #  ── Other Tools ───────────────────────────────────────────
    #  compare_database_schema: Show schema differences between branches
    compare_database_schema:
        rules: []
    #  complete_database_migration: Apply finalised migrations
    complete_database_migration:
        rules: []
    #  explain_sql_statement: Provide execution plans
    explain_sql_statement:
        rules: []
    #  prepare_database_migration: Initiate schema changes safely
    prepare_database_migration:
        rules: []
    #  provision_neon_auth: Set up authentication infrastructure
    provision_neon_auth:
        rules: []
    #  provision_neon_data_api: Enable HTTP database access
    provision_neon_data_api:
        rules: []
    #  reset_from_parent: Revert branch to parent state
    reset_from_parent:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
