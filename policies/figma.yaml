#  ─────────────────────────────────────────────────────────
#  Policy: Figma MCP Server
#  Server: figma/figma-mcp-server
#  Homepage: https://developers.figma.com/docs/figma-mcp-server
#  Tags: figma, design, ui, prototyping, design-systems
#  Tools: 7
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for figma/figma-mcp-server"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  get_code_connect_suggestions: Finds suggestions for mapping Figma node IDs to code components
    get_code_connect_suggestions:
        rules: []
    #  get_figjam: Returns metadata for FigJam diagrams in XML format including screenshots
    get_figjam:
        rules: []
    #  get_metadata: Returns a sparse XML representation of a Figma selection with layer IDs, names, types, and sizes
    get_metadata:
        rules: []
    #  ── Write Tools ───────────────────────────────────────────
    #  create_design_system_rules: Generates custom design system rules for the user's codebase
    create_design_system_rules:
        rules: []
    #  send_code_connect_mappings: Confirms the suggested Code Connect mappings
    send_code_connect_mappings:
        rules: []
    #  ── Other Tools ───────────────────────────────────────────
    #  generate_diagram: Generates a FigJam diagram from Mermaid syntax
    generate_diagram:
        rules: []
    #  whoami: Returns the identity of the authenticated Figma user
    whoami:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
