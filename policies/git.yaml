#  ─────────────────────────────────────────────────────────
#  Policy: Git MCP Server
#  Server: modelcontextprotocol/server-git
#  Homepage: https://github.com/modelcontextprotocol/servers/tree/main/src/git
#  Tags: git, version-control, repositories, commits
#  Tools: 14
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for modelcontextprotocol/server-git"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  git_show: Shows the contents of a commit
    git_show:
        rules: []
    #  ── Write Tools ───────────────────────────────────────────
    #  git_add: Adds file contents to the staging area
    git_add:
        rules: []
    #  git_create_branch: Creates a new branch from an optional base branch
    git_create_branch:
        rules: []
    #  ── Other Tools ───────────────────────────────────────────
    #  git_branch: List Git branches
    git_branch:
        rules: []
    #  git_checkout: Switches branches
    git_checkout:
        rules: []
    #  git_clone: Clones a repository
    git_clone:
        rules: []
    #  git_commit: Records changes to the repository
    git_commit:
        rules: []
    #  git_diff: Shows differences between branches or commits
    git_diff:
        rules: []
    #  git_diff_staged: Shows changes staged for the next commit
    git_diff_staged:
        rules: []
    #  git_diff_unstaged: Shows changes in the working directory not yet staged
    git_diff_unstaged:
        rules: []
    #  git_init: Initialises a new Git repository
    git_init:
        rules: []
    #  git_log: Shows the commit logs
    git_log:
        rules: []
    #  git_reset: Unstages all staged changes
    git_reset:
        rules: []
    #  git_status: Shows the working tree status
    git_status:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
