#  ─────────────────────────────────────────────────────────
#  Policy: ClickHouse MCP Server
#  Server: ClickHouse/mcp-clickhouse
#  Homepage: https://github.com/ClickHouse/mcp-clickhouse
#  Tags: clickhouse, analytics, database, sql, olap
#  Tools: 4
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for ClickHouse/mcp-clickhouse"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  list_databases: List all databases on ClickHouse cluster
    list_databases:
        rules: []
    #  list_tables: List tables in a database with pagination and optional metadata
    list_tables:
        rules: []
    #  ── Execute Tools ───────────────────────────────────────────
    #  run_chdb_select_query: Execute SQL queries using chDB embedded engine
    run_chdb_select_query:
        rules: []
    #  run_query: Execute SQL queries on ClickHouse cluster
    run_query:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
