#  ─────────────────────────────────────────────────────────
#  Policy: GitLab MCP Server
#  Server: gitlab-org/gitlab-mcp-server
#  Homepage: https://docs.gitlab.com/user/gitlab_duo/model_context_protocol/mcp_server_tools/
#  Tags: gitlab, repositories, merge-requests, ci-cd, issues
#  Tools: 15
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for gitlab-org/gitlab-mcp-server"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  get_issue: Retrieves detailed information about a specific issue
    get_issue:
        rules: []
    #  get_mcp_server_version: Returns the current version of the GitLab MCP server
    get_mcp_server_version:
        rules: []
    #  get_merge_request: Retrieves detailed information about a merge request
    get_merge_request:
        rules: []
    #  get_merge_request_commits: Retrieves the list of commits in a merge request
    get_merge_request_commits:
        rules: []
    #  get_merge_request_diffs: Retrieves the diffs for a merge request
    get_merge_request_diffs:
        rules: []
    #  get_merge_request_pipelines: Retrieves the pipelines for a merge request
    get_merge_request_pipelines:
        rules: []
    #  get_pipeline_jobs: Retrieves the jobs for a CI/CD pipeline
    get_pipeline_jobs:
        rules: []
    #  get_workitem_notes: Retrieves comments from a work item
    get_workitem_notes:
        rules: []
    #  gitlab_search: Searches across the GitLab instance
    gitlab_search:
        rules: []
    #  search_labels: Searches for labels in a project or group
    search_labels:
        rules: []
    #  ── Write Tools ───────────────────────────────────────────
    #  create_issue: Creates a new issue in a GitLab project
    create_issue:
        rules: []
    #  create_merge_request: Creates a merge request in a GitLab project
    create_merge_request:
        rules: []
    #  create_workitem_note: Adds a comment to a work item
    create_workitem_note:
        rules: []
    #  ── Other Tools ───────────────────────────────────────────
    #  manage_pipeline: Manages a GitLab CI/CD pipeline
    manage_pipeline:
        rules: []
    #  semantic_code_search: Searches for code snippets through semantic search
    semantic_code_search:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
