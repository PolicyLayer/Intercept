#  ─────────────────────────────────────────────────────────
#  Policy: N8n MCP Server
#  Server: czlonkowski/n8n-mcp
#  Homepage: https://github.com/czlonkowski/n8n-mcp
#  Tags: n8n, workflow-automation, no-code, integrations
#  Tools: 20
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for czlonkowski/n8n-mcp"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  tools_documentation: Fetch documentation for MCP tools and capabilities
    tools_documentation:
        rules: []
    #  search_nodes: Full-text search across all n8n nodes
    search_nodes:
        rules: []
    #  get_node: Get unified node info: docs, properties, versions
    get_node:
        rules: []
    #  search_templates: Search workflow templates by keyword/nodes/task
    search_templates:
        rules: []
    #  get_template: Retrieve complete workflow JSON from a template
    get_template:
        rules: []
    #  n8n_get_workflow: Retrieve a workflow by ID
    n8n_get_workflow:
        rules: []
    #  n8n_list_workflows: List workflows with filtering and pagination
    n8n_list_workflows:
        rules: []
    #  n8n_executions: Manage and view executions across workflows
    n8n_executions:
        rules: []
    #  n8n_health_check: Check connectivity and features of the n8n API
    n8n_health_check:
        rules: []
    #  ── Write Tools ───────────────────────────────────────────
    #  validate_node: Validate a node configuration
    validate_node:
        rules: []
    #  validate_workflow: Validate an entire workflow JSON
    validate_workflow:
        rules: []
    #  n8n_create_workflow: Create new workflows in your n8n instance
    n8n_create_workflow:
        rules: []
    #  n8n_update_full_workflow: Replace an existing workflow completely
    n8n_update_full_workflow:
        rules: []
    #  n8n_update_partial_workflow: Apply diff-based updates to a workflow
    n8n_update_partial_workflow:
        rules: []
    #  n8n_validate_workflow: Validate a workflow in-instance by ID
    n8n_validate_workflow:
        rules: []
    #  n8n_autofix_workflow: Automatically fix common workflow errors
    n8n_autofix_workflow:
        rules: []
    #  n8n_workflow_versions: Manage version history and rollbacks
    n8n_workflow_versions:
        rules: []
    #  n8n_deploy_template: Deploy n8n.io templates with auto-fix
    n8n_deploy_template:
        rules: []
    #  ── Execute Tools ───────────────────────────────────────────
    #  n8n_test_workflow: Trigger and test workflow execution
    n8n_test_workflow:
        rules: []
    #  ── Destructive Tools ───────────────────────────────────────────
    #  n8n_delete_workflow: Delete a workflow
    n8n_delete_workflow:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
