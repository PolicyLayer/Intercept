#  ─────────────────────────────────────────────────────────
#  Policy: Sentry MCP Server
#  Server: getsentry/sentry-mcp
#  Homepage: https://github.com/getsentry/sentry-mcp
#  Tags: sentry, error-tracking, monitoring, debugging
#  Tools: 25
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for getsentry/sentry-mcp"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  find_dsns: List all Sentry DSNs for a specific project
    find_dsns:
        rules: []
    #  find_organizations: Find organisations the user has access to
    find_organizations:
        rules: []
    #  find_projects: Find projects in a Sentry organisation
    find_projects:
        rules: []
    #  find_releases: Find releases in a Sentry organisation
    find_releases:
        rules: []
    #  find_teams: Find teams in an organisation
    find_teams:
        rules: []
    #  get_doc: Fetch the full markdown content of a Sentry documentation page
    get_doc:
        rules: []
    #  get_event_attachment: Download attachments from a Sentry event
    get_event_attachment:
        rules: []
    #  get_issue_details: Get detailed information about a specific issue
    get_issue_details:
        rules: []
    #  get_issue_tag_values: Get tag value distribution for a specific issue
    get_issue_tag_values:
        rules: []
    #  get_sentry_resource: Fetch a Sentry resource by URL or by type and ID
    get_sentry_resource:
        rules: []
    #  get_trace_details: Get detailed information about a specific trace
    get_trace_details:
        rules: []
    #  list_events: Search events using Sentry query syntax
    list_events:
        rules: []
    #  list_issue_events: List events within a specific issue
    list_issue_events:
        rules: []
    #  list_issues: List issues using Sentry query syntax
    list_issues:
        rules: []
    #  search_docs: Search Sentry documentation
    search_docs:
        rules: []
    #  search_events: Search for events and perform counts and aggregations
    search_events:
        rules: []
    #  search_issue_events: Search and filter events within a specific issue
    search_issue_events:
        rules: []
    #  search_issues: Search for grouped issues and problems in Sentry
    search_issues:
        rules: []
    #  ── Write Tools ───────────────────────────────────────────
    #  create_dsn: Create an additional DSN for an existing project
    create_dsn:
        rules: []
    #  create_project: Create a new project in Sentry
    create_project:
        rules: []
    #  create_team: Create a new team in Sentry
    create_team:
        rules: []
    #  update_issue: Update an issue's status or assignment in Sentry
    update_issue:
        rules: []
    #  update_project: Update project settings in Sentry
    update_project:
        rules: []
    #  ── Other Tools ───────────────────────────────────────────
    #  analyze_issue_with_seer: Use Seer to analyse production errors and get root cause analysis
    analyze_issue_with_seer:
        rules: []
    #  whoami: Identify the authenticated user in Sentry
    whoami:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
