#  ─────────────────────────────────────────────────────────
#  Policy: Semgrep MCP Server
#  Server: semgrep/mcp
#  Homepage: https://github.com/semgrep/mcp
#  Tags: semgrep, security, code-analysis, sast, vulnerability-scanning
#  Tools: 8
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for semgrep/mcp"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  get_abstract_syntax_tree: Output the Abstract Syntax Tree of code
    get_abstract_syntax_tree:
        rules: []
    #  ── Write Tools ───────────────────────────────────────────
    #  write_custom_semgrep_rule: Provide guidance for creating Semgrep rules
    write_custom_semgrep_rule:
        rules: []
    #  ── Other Tools ───────────────────────────────────────────
    #  security_check: Scan code for security vulnerabilities
    security_check:
        rules: []
    #  semgrep_findings: Fetch findings from the Semgrep AppSec Platform API
    semgrep_findings:
        rules: []
    #  semgrep_rule_schema: Fetch the latest Semgrep rule JSON Schema
    semgrep_rule_schema:
        rules: []
    #  semgrep_scan: Scan code files with a given config string
    semgrep_scan:
        rules: []
    #  semgrep_scan_with_custom_rule: Scan code files using a custom Semgrep rule
    semgrep_scan_with_custom_rule:
        rules: []
    #  supported_languages: Return the list of languages Semgrep supports
    supported_languages:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
