#  ─────────────────────────────────────────────────────────
#  Policy: Trigger.dev MCP Server
#  Server: triggerdotdev/trigger.dev
#  Homepage: https://trigger.dev
#  Tags: background-jobs, task-orchestration, durable-execution, deployment
#  Tools: 14
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for triggerdotdev/trigger.dev"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  search_docs: Search Trigger.dev documentation
    search_docs:
        rules: []
    #  list_orgs: List organisations
    list_orgs:
        rules: []
    #  list_projects: List projects
    list_projects:
        rules: []
    #  get_current_worker: Get current worker info
    get_current_worker:
        rules: []
    #  list_runs: List runs with filtering
    list_runs:
        rules: []
    #  get_run_details: Get details of a specific run
    get_run_details:
        rules: []
    #  list_deploys: List deployments
    list_deploys:
        rules: []
    #  list_preview_branches: List preview branches
    list_preview_branches:
        rules: []
    #  ── Write Tools ───────────────────────────────────────────
    #  create_project_in_org: Create a project within an org
    create_project_in_org:
        rules: []
    #  initialize_project: Initialise a Trigger.dev project
    initialize_project:
        rules: []
    #  ── Execute Tools ───────────────────────────────────────────
    #  trigger_task: Trigger a task run with a payload
    trigger_task:
        rules: []
    #  wait_for_run_to_complete: Wait for a run to finish
    wait_for_run_to_complete:
        rules: []
    #  cancel_run: Cancel an active run
    cancel_run:
        rules: []
    #  deploy: Deploy to an environment
    deploy:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
