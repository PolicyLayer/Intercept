#  ─────────────────────────────────────────────────────────
#  Policy: Graphiti MCP Server
#  Server: getzep/graphiti
#  Homepage: https://www.getzep.com
#  Tags: knowledge-graph, temporal-memory, agent-memory, neo4j, graph-rag
#  Tools: 9
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for getzep/graphiti"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  search_nodes: Search for entity nodes in the graph
    search_nodes:
        rules: []
    #  search_memory_facts: Search for facts/relationships in the graph
    search_memory_facts:
        rules: []
    #  get_episodes: Retrieve episodes from the graph
    get_episodes:
        rules: []
    #  get_entity_edge: Retrieve a specific entity edge by UUID
    get_entity_edge:
        rules: []
    #  get_status: Get health and connection status of the server
    get_status:
        rules: []
    #  ── Write Tools ───────────────────────────────────────────
    #  add_memory: Add an episode to the knowledge graph
    add_memory:
        rules: []
    #  ── Destructive Tools ───────────────────────────────────────────
    #  delete_entity_edge: Delete an entity edge from the graph
    delete_entity_edge:
        rules: []
    #  delete_episode: Delete an episode from the graph
    delete_episode:
        rules: []
    #  clear_graph: Clear all data for specified group IDs
    clear_graph:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
