#  ─────────────────────────────────────────────────────────
#  Policy: Neo4j MCP Server
#  Server: neo4j/mcp
#  Homepage: https://github.com/neo4j/mcp
#  Tags: neo4j, graph-database, cypher, knowledge-graph
#  Tools: 4
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for neo4j/mcp"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  get-schema: Introspect labels, relationship types, and property keys
    get-schema:
        rules: []
    #  list-gds-procedures: List Graph Data Science procedures available
    list-gds-procedures:
        rules: []
    #  read-cypher: Execute arbitrary Cypher queries in read mode
    read-cypher:
        rules: []
    #  ── Write Tools ───────────────────────────────────────────
    #  write-cypher: Execute Cypher with write capabilities
    write-cypher:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
