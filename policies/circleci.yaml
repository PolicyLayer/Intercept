#  ─────────────────────────────────────────────────────────
#  Policy: CircleCI MCP Server
#  Server: CircleCI-Public/mcp-server-circleci
#  Homepage: https://github.com/CircleCI-Public/mcp-server-circleci
#  Tags: circleci, ci-cd, testing, pipelines, deployment
#  Tools: 15
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for CircleCI-Public/mcp-server-circleci"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  find_flaky_tests: Identify flaky tests by analysing test execution history
    find_flaky_tests:
        rules: []
    #  find_underused_resource_classes: Find jobs with underused compute resources
    find_underused_resource_classes:
        rules: []
    #  get_build_failure_logs: Retrieve detailed failure logs from builds
    get_build_failure_logs:
        rules: []
    #  get_job_test_results: Retrieve test metadata and results for jobs
    get_job_test_results:
        rules: []
    #  get_latest_pipeline_status: Get status of latest pipeline for a branch
    get_latest_pipeline_status:
        rules: []
    #  list_component_versions: List all versions for a CircleCI component
    list_component_versions:
        rules: []
    #  list_followed_projects: List all CircleCI projects you are following
    list_followed_projects:
        rules: []
    #  ── Write Tools ───────────────────────────────────────────
    #  create_prompt_template: Generate structured prompt templates for AI applications
    create_prompt_template:
        rules: []
    #  ── Execute Tools ───────────────────────────────────────────
    #  run_pipeline: Trigger a pipeline to run
    run_pipeline:
        rules: []
    #  run_rollback_pipeline: Trigger a rollback for a project
    run_rollback_pipeline:
        rules: []
    #  ── Other Tools ───────────────────────────────────────────
    #  analyse_diff: Analyse git diffs against cursor rules for violations
    analyse_diff:
        rules: []
    #  config_helper: Validate and get guidance for CircleCI configuration
    config_helper:
        rules: []
    #  download_usage_api_data: Download usage data from CircleCI Usage API
    download_usage_api_data:
        rules: []
    #  recommend_prompt_template_tests: Generate test cases for prompt templates
    recommend_prompt_template_tests:
        rules: []
    #  rerun_workflow: Rerun workflow from start or from failed job
    rerun_workflow:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
