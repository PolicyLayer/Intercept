#  ─────────────────────────────────────────────────────────
#  Policy: Convex MCP Server
#  Server: get-convex/convex-backend
#  Homepage: https://convex.dev
#  Tags: database, reactive, serverless, typescript, functions
#  Tools: 12
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for get-convex/convex-backend"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  status: Query available deployments
    status:
        rules: []
    #  tables: List all tables with declared/inferred schemas
    tables:
        rules: []
    #  data: Paginate through documents in a table
    data:
        rules: []
    #  runOneoffQuery: Execute sandboxed read-only JS queries
    runOneoffQuery:
        rules: []
    #  functionSpec: Get metadata for all deployed functions
    functionSpec:
        rules: []
    #  logs: Retrieve recent function execution log entries
    logs:
        rules: []
    #  insights: Fetch health insights for the deployment
    insights:
        rules: []
    #  envList: List all environment variables for a deployment
    envList:
        rules: []
    #  envGet: Get a specific environment variable's value
    envGet:
        rules: []
    #  ── Write Tools ───────────────────────────────────────────
    #  envSet: Set or update an environment variable
    envSet:
        rules: []
    #  ── Execute Tools ───────────────────────────────────────────
    #  run: Execute a deployed Convex function with provided arguments
    run:
        rules: []
    #  ── Destructive Tools ───────────────────────────────────────────
    #  envRemove: Remove an environment variable
    envRemove:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
