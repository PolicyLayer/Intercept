#  ─────────────────────────────────────────────────────────
#  Policy: Firecrawl MCP Server
#  Server: firecrawl/firecrawl-mcp-server
#  Homepage: https://github.com/mendableai/firecrawl-mcp-server
#  Tags: firecrawl, web-scraping, crawling, extraction
#  Tools: 7
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for firecrawl/firecrawl-mcp-server"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  firecrawl_search: Web search to locate information across the internet
    firecrawl_search:
        rules: []
    #  ── Other Tools ───────────────────────────────────────────
    #  firecrawl_agent: Handle complex multi-source research tasks
    firecrawl_agent:
        rules: []
    #  firecrawl_batch_scrape: Process multiple URLs efficiently with rate limiting
    firecrawl_batch_scrape:
        rules: []
    #  firecrawl_browser: Interactive browser automation
    firecrawl_browser:
        rules: []
    #  firecrawl_crawl: Multi-page extraction for entire site sections
    firecrawl_crawl:
        rules: []
    #  firecrawl_map: Discover URLs by mapping website structure and links
    firecrawl_map:
        rules: []
    #  firecrawl_scrape: Extract content from a single URL
    firecrawl_scrape:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
