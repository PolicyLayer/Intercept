#  ─────────────────────────────────────────────────────────
#  Policy: Blender MCP Server
#  Server: ahujasid/blender-mcp
#  Homepage: https://github.com/ahujasid/blender-mcp
#  Tags: blender, 3d-modeling, python, creative, scene-manipulation
#  Tools: 22
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for ahujasid/blender-mcp"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  get_scene_info: Get detailed info about the current Blender scene
    get_scene_info:
        rules: []
    #  get_object_info: Get info about a specific object in the scene
    get_object_info:
        rules: []
    #  get_viewport_screenshot: Capture a screenshot of the 3D viewport
    get_viewport_screenshot:
        rules: []
    #  get_polyhaven_categories: List asset categories on Polyhaven
    get_polyhaven_categories:
        rules: []
    #  get_polyhaven_status: Check if PolyHaven integration is enabled
    get_polyhaven_status:
        rules: []
    #  get_hyper3d_status: Check if Hyper3D Rodin integration is enabled
    get_hyper3d_status:
        rules: []
    #  get_sketchfab_status: Check if Sketchfab integration is enabled
    get_sketchfab_status:
        rules: []
    #  get_hunyuan3d_status: Check if Hunyuan3D integration is enabled
    get_hunyuan3d_status:
        rules: []
    #  search_polyhaven_assets: Search Polyhaven assets with optional filters
    search_polyhaven_assets:
        rules: []
    #  search_sketchfab_models: Search Sketchfab models
    search_sketchfab_models:
        rules: []
    #  get_sketchfab_model_preview: Get a thumbnail for a Sketchfab model
    get_sketchfab_model_preview:
        rules: []
    #  poll_rodin_job_status: Check Hyper3D Rodin generation status
    poll_rodin_job_status:
        rules: []
    #  poll_hunyuan_job_status: Check Hunyuan3D job status
    poll_hunyuan_job_status:
        rules: []
    #  ── Write Tools ───────────────────────────────────────────
    #  set_texture: Apply a downloaded Polyhaven texture to an object
    set_texture:
        rules: []
    #  download_polyhaven_asset: Download and import a Polyhaven asset
    download_polyhaven_asset:
        rules: []
    #  download_sketchfab_model: Download and import a Sketchfab model
    download_sketchfab_model:
        rules: []
    #  import_generated_asset: Import a Hyper3D-generated asset
    import_generated_asset:
        rules: []
    #  import_generated_asset_hunyuan: Import a Hunyuan3D-generated asset
    import_generated_asset_hunyuan:
        rules: []
    #  ── Execute Tools ───────────────────────────────────────────
    #  execute_blender_code: Execute arbitrary Python code in Blender
    execute_blender_code:
        rules: []
    #  generate_hyper3d_model_via_text: Generate 3D asset via Hyper3D from text
    generate_hyper3d_model_via_text:
        rules: []
    #  generate_hyper3d_model_via_images: Generate 3D asset via Hyper3D from images
    generate_hyper3d_model_via_images:
        rules: []
    #  generate_hunyuan3d_model: Generate 3D asset via Hunyuan3D
    generate_hunyuan3d_model:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
