#  ─────────────────────────────────────────────────────────
#  Policy: MongoDB MCP Server
#  Server: mongodb-js/mongodb-mcp-server
#  Homepage: https://github.com/mongodb-js/mongodb-mcp-server
#  Tags: mongodb, database, nosql, atlas, aggregation
#  Tools: 36
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for mongodb-js/mongodb-mcp-server"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  atlas-get-performance-advisor: Get Atlas performance advisor recommendations
    atlas-get-performance-advisor:
        rules: []
    #  atlas-list-alerts: List MongoDB Atlas alerts
    atlas-list-alerts:
        rules: []
    #  atlas-list-clusters: List MongoDB Atlas clusters
    atlas-list-clusters:
        rules: []
    #  atlas-list-db-users: List MongoDB Atlas database users
    atlas-list-db-users:
        rules: []
    #  atlas-list-orgs: List MongoDB Atlas organisations
    atlas-list-orgs:
        rules: []
    #  atlas-list-projects: List MongoDB Atlas projects
    atlas-list-projects:
        rules: []
    #  find: Run a find query against a MongoDB collection
    find:
        rules: []
    #  list-collections: List all collections for a given database
    list-collections:
        rules: []
    #  list-databases: List all databases for a MongoDB connection
    list-databases:
        rules: []
    #  ── Write Tools ───────────────────────────────────────────
    #  atlas-create-access-list: Allow IP/CIDR ranges to access Atlas clusters
    atlas-create-access-list:
        rules: []
    #  atlas-create-db-user: Create a MongoDB Atlas database user
    atlas-create-db-user:
        rules: []
    #  atlas-create-free-cluster: Create a free MongoDB Atlas cluster
    atlas-create-free-cluster:
        rules: []
    #  atlas-create-project: Create a MongoDB Atlas project
    atlas-create-project:
        rules: []
    #  create-collection: Creates a new collection in a database
    create-collection:
        rules: []
    #  create-index: Create an index for a collection
    create-index:
        rules: []
    #  insert-many: Insert an array of documents into a collection
    insert-many:
        rules: []
    #  rename-collection: Renames a collection in a database
    rename-collection:
        rules: []
    #  update-many: Updates all documents that match the filter
    update-many:
        rules: []
    #  ── Destructive Tools ───────────────────────────────────────────
    #  delete-many: Removes all documents that match the filter
    delete-many:
        rules: []
    #  drop-collection: Removes a collection or view from the database
    drop-collection:
        rules: []
    #  drop-database: Removes the specified database
    drop-database:
        rules: []
    #  drop-index: Drop an index for a collection
    drop-index:
        rules: []
    #  ── Other Tools ───────────────────────────────────────────
    #  aggregate: Run an aggregation against a MongoDB collection
    aggregate:
        rules: []
    #  atlas-connect-cluster: Connect to a MongoDB Atlas cluster
    atlas-connect-cluster:
        rules: []
    #  atlas-inspect-access-list: Inspect IP/CIDR ranges with access to Atlas
    atlas-inspect-access-list:
        rules: []
    #  atlas-inspect-cluster: Inspect metadata of a MongoDB Atlas cluster
    atlas-inspect-cluster:
        rules: []
    #  collection-indexes: Describe the indexes for a collection
    collection-indexes:
        rules: []
    #  collection-schema: Describe the schema for a collection
    collection-schema:
        rules: []
    #  collection-storage-size: Gets the size of the collection
    collection-storage-size:
        rules: []
    #  connect: Connect to a MongoDB instance
    connect:
        rules: []
    #  count: Gets the number of documents in a collection
    count:
        rules: []
    #  db-stats: Returns statistics for a single database
    db-stats:
        rules: []
    #  explain: Returns execution statistics for the winning query plan
    explain:
        rules: []
    #  export: Export query or aggregation results in EJSON format
    export:
        rules: []
    #  mongodb-logs: Returns the most recent logged mongod events
    mongodb-logs:
        rules: []
    #  switch-connection: Switch to a different MongoDB connection
    switch-connection:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
