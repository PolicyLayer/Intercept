#  ─────────────────────────────────────────────────────────
#  Policy: Cognee MCP Server
#  Server: topoteretes/cognee
#  Homepage: https://cognee.ai
#  Tags: knowledge-graph, ai-memory, semantic-search, entity-extraction
#  Tools: 7
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for topoteretes/cognee"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  search: Query the knowledge graph using various search types
    search:
        rules: []
    #  list_data: List all datasets or data items in a given dataset
    list_data:
        rules: []
    #  cognify_status: Get the current processing status of a cognify job
    cognify_status:
        rules: []
    #  ── Write Tools ───────────────────────────────────────────
    #  save_interaction: Log user-agent interactions into the knowledge graph
    save_interaction:
        rules: []
    #  ── Execute Tools ───────────────────────────────────────────
    #  cognify: Transform ingested data into a structured knowledge graph
    cognify:
        rules: []
    #  ── Destructive Tools ───────────────────────────────────────────
    #  delete: Delete a data item by ID from a dataset
    delete:
        rules: []
    #  prune: Clear all stored data and reset the knowledge graph
    prune:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
