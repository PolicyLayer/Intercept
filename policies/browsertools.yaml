#  ─────────────────────────────────────────────────────────
#  Policy: BrowserTools MCP Server
#  Server: AgentDeskAI/browser-tools-mcp
#  Homepage: https://browsertools.agentdesk.ai
#  Tags: browser-monitoring, chrome-extension, debugging, auditing
#  Tools: 15
#  Generated: 2026-03-04T16:57:07Z

#  Generated by Intercept — https://intercept.policylayer.com
#  Control what AI agents can do — enforce policies on every MCP tool call.
#  ─────────────────────────────────────────────────────────
version: "1"
description: "Policy for AgentDeskAI/browser-tools-mcp"
#  Set to "deny" to reject tool calls not listed below
default: "allow"
tools:
    #  ── Read Tools ───────────────────────────────────────────
    #  getConsoleLogs: Retrieve recent browser console log entries
    getConsoleLogs:
        rules: []
    #  getConsoleErrors: Retrieve recent console error messages
    getConsoleErrors:
        rules: []
    #  getNetworkLogs: Get all XHR/fetch network logs
    getNetworkLogs:
        rules: []
    #  getNetworkErrors: Get failed HTTP requests (4xx/5xx)
    getNetworkErrors:
        rules: []
    #  getNetworkSuccess: Get successful HTTP requests (2xx/3xx)
    getNetworkSuccess:
        rules: []
    #  getSelectedElement: Get currently selected DOM element from DevTools
    getSelectedElement:
        rules: []
    #  takeScreenshot: Take a screenshot of the active tab
    takeScreenshot:
        rules: []
    #  ── Execute Tools ───────────────────────────────────────────
    #  runAccessibilityAudit: Run WCAG accessibility audit via Lighthouse
    runAccessibilityAudit:
        rules: []
    #  runPerformanceAudit: Run performance audit
    runPerformanceAudit:
        rules: []
    #  runSEOAudit: Run SEO audit
    runSEOAudit:
        rules: []
    #  runBestPracticesAudit: Run web best-practices audit
    runBestPracticesAudit:
        rules: []
    #  runNextJSAudit: Run Next.js-specific audit
    runNextJSAudit:
        rules: []
    #  runDebuggerMode: Run all debugging tools in sequence
    runDebuggerMode:
        rules: []
    #  runAuditMode: Run all audit tools in sequence
    runAuditMode:
        rules: []
    #  ── Destructive Tools ───────────────────────────────────────────
    #  wipeLogs: Clear all stored logs from the local server
    wipeLogs:
        rules: []

  # Uncomment to apply a global rate limit across all tools:
  # "*":
  #   rules:
  #     - name: "global rate limit"
  #       rate_limit: 60/minute
